---
version: 1
description: Capture, evolve, promote, or drop an exploration in `.cumaru/exploring/`. Drives the `cumaru-explore` skill.
allowed-tools: Bash, Read, Edit, Write
argument-hint: <slug-or-action>
summary: Capture, evolve, promote, or drop an exploration in `.cumaru/exploring/`. Drives the `cumaru-explore` skill.
---

Arguments: `$ARGUMENTS`

Load the installed `cumaru-explore` skill and follow its workflow using the
arguments above. The skill is the canonical source; do not duplicate or bypass
its recipe here.
