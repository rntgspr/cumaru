---
version: 1
description: Bootstrap, deepen, split, or consolidate specifications.
human_revised: false
summary: Framework command guidance for `/cumaru:specs` workflow execution.
---

Arguments: `$ARGUMENTS`

Load the installed `cumaru-specs` skill and follow its workflow using the
arguments above. The skill is the canonical source; do not duplicate or bypass
its recipe here.
