---
human_revised: false
name: <area discovery>
summary: Working discovery notes for a design area inferred from existing evidence.
depends-on: [] # relevant paths under specs/
targets: [] # component keys from config.yaml
---

# Discovery — <area>

Optional working notes authored by Design Lead through `cumaru-specs`.
No CLI generates this content. Use `templates/spec.md` for the durable contract;
these notes hold unresolved questions, not a second requirements store.

Set `name` to a concise discovery concern, `depends-on` to the relevant spec
paths, and `targets` to the affected component keys declared in `config.yaml`.

## Inspected sources

Record source files, existing design documents, prototype revisions, and checks
actually inspected, with dates and limitations.

## Findings and open questions

Separate observed behavior from inferred intent. Link accepted findings to the
owning spec requirement and retain needed provenance there. Record unanswered
questions explicitly; do not claim unverified implementation behavior.

## Next discovery scope

Name only relevant concerns requiring a deeper read. Replace resolved notes
with links to current spec owners instead of appending a permanent work log.
