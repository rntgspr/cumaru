The outer body contains ordinary adopter prose before a nested tag.

<!-- cumaru:inner -->
The inner tag remains independently addressable while also belonging to the
complete body returned for `outer`.

| Field | Value |
|---|---|
| parsing | non-recursive |
<!-- /cumaru:inner -->

The outer body continues after the nested tag and retains every byte through
its own exact closing marker.
