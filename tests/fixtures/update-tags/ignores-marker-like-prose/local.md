# Local document

The string `<!-- cumaru:not-a-tag -->` is an example, not a delimiter. The local
wording differs so the merge proves that only the actual body is retained. The
same applies to `<!-- /cumaru:not-a-tag -->` and to the complete inline pair
`<!-- cumaru:sample -->content<!-- /cumaru:sample -->`.

<!-- cumaru:real-tag -->
The real body contains adopter decisions that survive canonical prose updates.

It includes enough content to expose accidental truncation or normalization.
<!-- /cumaru:real-tag -->
